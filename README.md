
  # Recreate Screen Flow2

  This is a code bundle for Recreate Screen Flow2. The original project is available at https://www.figma.com/design/N22Bw4B89hupwdiRHVwwda/Recreate-Screen-Flow2.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  